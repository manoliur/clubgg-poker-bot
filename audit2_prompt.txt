Ты — аудитор покерного бота для ClubGG (кэш NLH, стол до 6 игроков, Android-телефон). Бот: cv2-распознавание карт + табличная стратегия (БЕЗ ИИ в игровом цикле — это принципиально). Код в текущей директории.

КОНТЕКСТ ПОСЛЕДНИХ ИЗМЕНЕНИЙ (коммит b0608b6):
1. Раньше любой флеш/стрит считался 'nuts' (cat >= FLUSH → made='nuts'). Исправлено: сила флешей/стритов по рангу НАШЕЙ карты масти — nuts (A), strong (K/Q), medium (T/9/8/7), weak (6-). Младшие стриты не nuts.
2. Шансы банка: колл алл-ина только если пот-оддсы < эквити (weak ~25-35%, medium ~40-50%, strong ~60-75%, nuts ~85%+). Лог показывает «пот-оддсы X%, эквити ~Y%».
3. Раньше «raise невозможен» подменялся на call вслепую — теперь стратегия решает по пот-оддсам (3-max алл-ин 23.7ББ с 76s → FOLD).

ТВОЯ ЗАДАЧА — АУДИТ + ДОКРУТКА:
1. Прочитай main.py, strategy.py, hand_evaluator.py, table_state.py, card_reader.py, tests/.
2. Найди, что ещё докрутить в ЛОГИКЕ РЕШЕНИЙ (главный фокус пользователя):
   - шансы банка/пот-оддсы: корректны ли расчёты, правильно ли сравниваются с эквити, учтены ли неявные пот-оддсы/вложенные банки, лимиты ставок
   - классификация силы руки: краевые случаи (двусторонние дро, оверкарты, кикер, пара доски, фулл-хаус из мелкой пары доски, сплит-поты, мертвые карты)
   - размеры ставок: всегда ли мин-бет, нет ли логики выбора размера (1/2 пота, 2/3, 3/4)
   - вэлью/блеф/фолд-эквити в табличной стратегии
   - поведение на ривере vs терне (флеш-дро не собралось и т.п.)
   - олл-ин ситуации, защита стека (ICM не нужен, это кэш)
   - спец-случаи: хедз-ап vs 6-max, короткие столы
3. Общий поиск багов: краши (пустые списки, None), бесконечные циклы, неверные индексы.
4. СДЕЛАЙ ПРАВКИ сам: только то, что явно улучшает игру и покрыто тестами. Не меняй: координаты кнопок/тапов, принцип «без ИИ в цикле», структуру панели. Не рефактори ради рефакторинга.
5. Добавь/обнови тесты. Запусти pytest — ВСЕ тесты должны проходить (сейчас 198 passed).
6. Коммитни с понятным сообщением. В ФИНАЛЬНОМ ОТВЕТЕ (русский): вердикт по приоритетам (критично/важно/мелочь), что исправил, что решил не трогать и почему.

Ограничения: файлы-скриншоты читай ТОЛЬКО из shots_small/ (уменьшенные). Не запускай бота. Не используй --dangerously-skip-permissions (запрещено под root).
